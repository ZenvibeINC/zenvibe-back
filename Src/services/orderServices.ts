
export const createOrderService = async( ) => {
    //input (userId, userData)
    //logica para crear un pedido
}

export const getOrderHistoryService = async () => {
    //input (userId)
    //logica para obtener historial de pedidos
}